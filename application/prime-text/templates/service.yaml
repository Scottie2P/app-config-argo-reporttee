apiVersion: v1
kind: Service
metadata:
  name: {{ include "prime-text.fullname" . }}
  labels:
    {{- include "prime-text.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prime-text.selectorLabels" . | nindent 4 }}
    datawow.io/component: api
