apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prime-text.fullname" . }}-test-connection"
  labels:
    {{- include "prime-text.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prime-text.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
